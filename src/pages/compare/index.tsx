import Table from "./table";

export default () => (
    <div class={'w-full'}>
        <p class={'text-5xl font-extrabold'}>How We Compare</p>
        <p class={'text-xl text-gray-500 mb-8 mt-1'}>See our comparison table for a comparison against competitors.</p>
        <Table />
    </div>
)